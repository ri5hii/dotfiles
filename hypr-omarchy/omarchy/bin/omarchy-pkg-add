#!/bin/bash

# omarchy:summary=Install packages if they are missing (distro-agnostic via omarchy-pm)
# omarchy:args=<packages...>
# omarchy:examples=omarchy pkg add jq ripgrep
# omarchy:requires-sudo=true

if omarchy-pm is-missing "$@"; then
  omarchy-pm install "$@" || exit 1
fi

exit 0