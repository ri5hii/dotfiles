#!/bin/bash

# omarchy:summary=Show a fuzzy-finder TUI for picking packages installed on the system to be removed (distro-agnostic).
# omarchy:requires-sudo=true

backend=$(omarchy-pm backend)

case "$backend" in
  pacman)
    pkg_list_cmd='yay -Qqe'
    preview_cmd='yay -Qi {1}'
    ;;
  dnf)
    pkg_list_cmd='rpm -qa --qf "%{NAME}\n"'
    preview_cmd='dnf info {1}'
    ;;
  apt)
    pkg_list_cmd='dpkg-query -W -f="${Package}\n"'
    preview_cmd='apt-cache show {1} | head -n 40'
    ;;
  flatpak)
    pkg_list_cmd='flatpak list --app --columns=application'
    preview_cmd='flatpak info {1}'
    ;;
  *)
    echo "Error: no supported package manager found" >&2
    exit 1
    ;;
esac

fzf_args=(
  --multi
  --preview "$preview_cmd"
  --preview-label='alt-p: toggle description, alt-j/k: scroll, tab: multi-select'
  --preview-label-pos='bottom'
  --preview-window 'down:65%:wrap'
  --bind 'alt-p:toggle-preview'
  --bind 'alt-d:preview-half-page-down,alt-u:preview-half-page-up'
  --bind 'alt-k:preview-up,alt-j:preview-down'
  --color 'pointer:red,marker:red'
)

pkg_names=$(eval "$pkg_list_cmd" | fzf "${fzf_args[@]}")

if [[ -n $pkg_names ]]; then
  # Convert newline-separated selections to space-separated for the backend
  echo "$pkg_names" | tr '\n' ' ' | xargs omarchy-pm remove
  omarchy-show-done
fi